% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_ortho.R
\name{backtracking_line_search_max}
\alias{backtracking_line_search_max}
\title{Backtracking Line Search for Maximization}
\usage{
backtracking_line_search_max(
  f,
  grad_i,
  x,
  i,
  direction = 1,
  alpha = 1,
  beta = 0.5,
  c = 1e-04,
  TOL = 1e-08,
  parameter.set = "sigma2"
)
}
\arguments{
\item{f}{A function to be maximized. Should accept a numeric vector of parameters.}

\item{grad_i}{The partial derivative (gradient) with respect to the \code{i}-th coordinate at the current point.}

\item{x}{A numeric vector of current parameter values.}

\item{i}{An integer index specifying the coordinate to update.}

\item{direction}{The direction of the update (usually 1). Negative values reverse the gradient direction.}

\item{alpha}{Initial step size (default is 1).}

\item{beta}{Step size reduction factor (default is 0.5; must be in (0, 1)).}

\item{c}{Armijo condition constant (default is 1e-4; must be small and positive).}

\item{TOL}{Small positive number to enforce non-negativity constraint (default is 1e-8).}

\item{parameter.set}{A string indicating the parameterization:
'h2' for proportions of variance (h2_i) and total variance (s2), or
'sigma2' for direct variance components (s21, s22, ..., s2e).}
}
\value{
A numeric value giving the step size that satisfies the backtracking line search condition.
}
\description{
Performs backtracking line search along a single coordinate direction to find a step size
that satisfies an Armijo-type condition for maximization.
}
\details{
This function is typically used in coordinate ascent algorithms where the objective function
is differentiable and maximization is desired. The step is accepted when the function value
increases sufficiently compared to the linear approximation.
}
